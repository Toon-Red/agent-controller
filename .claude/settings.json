{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Project-level Claude Code settings for agent-controller. Tracked. Local-only overrides go in .claude/settings.local.json. SessionStart hook reports the claude-flow npm dep status.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python .claude/hooks/health_check.py",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
